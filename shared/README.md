# Share

> Cannot think of a better name

Here is the folder for reusable components such as `HealthComponent` or `StateManagement`

This is also where the parent of an inherited component are such as `Enemy` component or `Projectile` component
